{
  "name": "bc-st-cyprien",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "prebuild": "node scripts/apply-content.mjs && node scripts/build-avif.mjs && node scripts/build-sitemap.mjs",
    "build": "astro build",
    "postbuild": "node scripts/cuire-galerie.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.0.0"
  }
}
